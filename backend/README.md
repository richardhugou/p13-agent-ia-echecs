# backend

API FastAPI + agent LangGraph du POC. Voir le README racine pour la vue d'ensemble.

```bash
uv sync          # installe l'environnement
uv run pytest    # tests
uv run ruff check .   # lint
```
